Apply a consistent logging and output:
1. P(dcpa < 50m), P(dcpa < 25m), P(dcpa < 10m), P(dcpa < 5m)
2. How about efficiency metric? This can't be done with IPS because IPS removes the non-colliding-particle. Maybe a separate simulation, a cheap MC, the N jobs can be tuned
3. 