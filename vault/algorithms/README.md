# algorithms

One note per algorithm — detection, resolution, recovery, estimators — each pointing to the
module that implements it. A contributor reads the note to understand and trust the code
(design_brief.md: the interface is the contribution surface).
